# Create an XDP chain
chain this_is_a_long_chain_name_is_it_too_long BF_HOOK_XDP{ifindex=2} ACCEPT
    rule
        meta.dport eq 22
        log internet
        counter
        ACCEPT

chain this_is_a_long_chaiwefewn_name_is_it_too_long BF_HOOK_CGROUP_INGRESS{cgpath=/sys/fs/cgroup} ACCEPT
    rule
        meta.dport eq 22
        log internet
        counter
        ACCEPT

chain this_is_a_longwef_chain_name_is_it_too_long BF_HOOK_CGROUP_INGRESS{cgpath=/sys/fs/cgroup} ACCEPT
    rule
        meta.dport eq 22
        log internet
        counter
        ACCEPT

chain this_is_a_long_wefchain_name_is_it_too_long BF_HOOK_CGROUP_INGRESS{cgpath=/sys/fs/cgroup} ACCEPT
    rule
        meta.dport eq 22
        log internet
        counter
        ACCEPT

chain this_is_a_long_cwefhain_name_is_it_too_long BF_HOOK_CGROUP_INGRESS{cgpath=/sys/fs/cgroup} ACCEPT
    rule
        meta.dport eq 22
        log internet
        counter
        ACCEPT
