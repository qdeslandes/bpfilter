# Create a cgroup chain
chain BF_HOOK_CGROUP_INGRESS{cgroup=/sys/fs/cgroup/user.slice,attach=yes} policy ACCEPT
    rule
        meta.l3_proto ipv4 counter ACCEPT
    rule
        meta.l3_proto ipv6 counter ACCEPT
