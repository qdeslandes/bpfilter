#!/usr/bin/env python3
"""CLI entry point — delegates to bfbencher_lib.main()."""

import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent))

from bfbencher_lib import main  # noqa: E402

if __name__ == "__main__":
    main()
